import styled from "styled-components";
import { Typography } from "@mui/material";

// example of a styled component that will be used in the home page.
export const HomePageTitle = styled(Typography)`
  color: #1976d2;
  margin-bottom: 16px;
  font-weight: 700;
`;
